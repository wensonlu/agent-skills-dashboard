{
  "name": "agent-skills-dashboard",
  "version": "0.1.8",
  "private": true,
  "description": "AI Coding Skills Visualization Dashboard - AI Agent oriented monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "build:demo": "pnpm tsx scripts/generate-demo-data.ts",
    "demo": "pnpm --filter agent-skills-dashboard-demo dev",
    "test": "pnpm -r test",
    "dev": "pnpm -r --parallel dev"
  },
  "keywords": [
    "agent-skills",
    "claude-code",
    "ai-coding",
    "skills-dashboard"
  ],
  "license": "MIT",
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
